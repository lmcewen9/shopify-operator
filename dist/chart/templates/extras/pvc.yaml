apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "shopify-operator.resourceName" (dict "suffix" "pvc" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: standard
